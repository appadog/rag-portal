# RAG 포탈 기능 개발 계획

> 실무 개발 시 참고용 스펙 문서. Wave-074에서 최초 작성, 이후 대화(커맨더-사용자)에서 나온 설계 결정을 반영해 갱신.
> RAG 개념/기법 자체에 대한 리서치 내용은 [`RAG_PORTAL_GUIDE.md`](./RAG_PORTAL_GUIDE.md) 참고 — 이 문서는 "우리가 무엇을 만들지, 왜 그렇게 만들기로 했는지"에 집중.

---

## 0. 현재 구현 감사와 플로우별 다음 개발

> 2026-07 기준 코드 감사 결과. 아래 표가 다음 스프린트의 기준 백로그다. 뒤의 장은 최초 설계의 근거를 보존하며, 현재 구현 상태와 다른 설명이 있을 경우 이 절을 우선한다.

### 0.1 완료된 제품 플로우

1. **생성** — 질문지로 임베딩 모델을 추천하고, 사용자가 선택한 하나의 모델을 RAG 인스턴스에 고정한다.
2. **업로드·준비** — TXT/PDF/DOCX/XLSX를 파싱하고, OCR이 가능한 환경에서는 스캔 문서도 처리한다. 작업은 SQLite에 남는 job으로 실행하며 폴링·취소·재시도를 지원한다.
3. **후보 생성** — 문단·제목·표 형태·문서 길이·OCR 여부를 분석해 문서별 청킹 전략 3개와 실제 파라미터를 계산하고, 각 전략에 검색 방식 3개를 연결한다.
4. **비교·확정** — 후보별 근거 기반 답변과 인라인 인용을 비교하고, 복수 투표·단독 1위 확정·미확정 후보 정리를 수행한다.
5. **실사용·재방문** — 확정된 문서 범위를 선택해 검색하고, 인용 원문을 열며, 피드백·재튜닝 신호·산출물 이력을 저장한다.
6. **모델 계약** — local TEI embedding/reranker, OCR, Redis의 readiness와 인스턴스별 실행 계획을 API로 노출한다.

### 0.2 다음 개발 백로그 — 사용자 플로우·의존성 기준

완료 여부만 나열하지 않고, **앞 작업의 산출물이 다음 작업의 입력이 되는 순서**로 관리한다. `P0`는 다음 스프린트의 릴리스 조건이며, `P1`부터는 P0 결과가 확인된 뒤 착수한다.

| Sprint | 상태 | 우선순위 | 플로우 | 남은 작업 | 선행 조건 | 종료 조건 |
| --- | --- | --- | --- | --- | --- | --- |
| 06 | ✅ | P0 | 업로드 → 후보 준비 | 후보 준비/실패/근거 없음 상태와 복구 | 없음 | snapshot 복원, 선택 제한, 재시도 완료 |
| 07 | 🟡 | P0 | 품질 검증 → 모델 선택 | 실제 corpus 10–20개와 golden Q/A·근거 라벨로 benchmark 확대 | 실제 모델 runtime | BGE/Qwen/Gemma 실측이 추천 근거로 조회됨 |
| 08 | 🟡 | P0 | 대형 문서 업로드 → 비교 | 실제 대형 PDF/DOCX로 sample/full 재인덱싱 E2E | 실제 모델 runtime | 범위·비용·재인덱싱 상태가 실제 파일에서도 정확함 |
| 09 | 🟡 | P1 | 비교·검색 → grounded 답변 | 실제 generator endpoint latency·취소·잘못된 citation fallback smoke | 실제 generator runtime | 제공 citation 밖의 모델 문장이 노출되지 않음 |
| 10 | 🟡 | P1 | 여러 문서 → 답변 | 실제 다문서 corpus에서 정규화·rerank·종합 citation 품질 E2E | 07, 09 | 서로 다른 파이프라인의 근거가 하나의 답변에 보존됨 |
| 11 | 🟡 구현·자동 QA 완료 / runtime gate 대기 | **P0 release gate** | 모델 준비 → 릴리스 | 실제 모델 기동, provider smoke, 모델 미준비·지연·OCR/후보 실패·대형 문서·재방문 E2E | 06–10 | endpoint가 준비되면 `release_gate.sh`로 happy/recovery path 확인 |
| 12 | ✅ 구현·자동 QA 완료 | P1 | 피드백 → 재튜닝 | 피드백·stored answer facts 기반 임계값 보정, 전후 artifact, 추천 사유 UI | 07, 11 구현 | 운영자가 재튜닝 효과를 비교 가능; 실제 benchmark/runtime 근거는 release gate 뒤 확인 |
| 13 | ✅ 구현·자동 QA 완료 | P1 | 문서 관리 → 재방문 | immutable source storage adapter, checksum·중복 처리, parser/chunk/model 버전, 재파싱 | 08, 11 | 원본 복구·재처리·재현 가능; 실제 object storage gateway smoke는 운영 gate |
| 14 | ✅ 구현·자동 QA 완료 | P1 | job 실행 → 운영 | durable job state, Redis/SQS-compatible dispatch, 멱등성·재시도·dead-letter·관측성 | 06, 11 | 다중 worker·장애 복구 상태 계약 보장; 실제 broker/worker smoke는 운영 gate |
| 15 | ✅ 구현·자동 QA 완료 | P2 | 후보 비교 → 개인화 | bounded 후보 축소·파라미터 제안·ledger·명시적 되돌림 | 07, 12 | 탐색 비용을 줄이고 근거·되돌림 보존, 자동 선택·확정 없음 |

### 0.3 재개용 권장 다음 스프린트 — 실제 Runtime·운영 Release Gate

Sprint 12–15의 코드·자동 QA까지 완료된 현재, 다음 작업은 새 기능보다 **실제 모델·실제 파일·실제 broker/object storage로 Sprint 07–15 계약을 릴리스 가능 상태로 검증**하는 것이다. 개발용 fallback이나 mock 결과는 성공으로 간주하지 않는다.

1. BGE-M3, reranker, generator, Tesseract를 기동하고 `model-runtime`·각 인스턴스 `execution-plan`의 필수 서비스가 모두 `READY`인지 확인한다.
2. 대표 문서 10–20개와 golden 질문·정답 근거를 versioned fixture로 확정하고 benchmark를 다시 실행한다.
3. 단일·대형·다문서·스캔 문서로 업로드 → 비교 → 확정 → 검색 E2E를 실행한다.
4. Redis/SQS 중 하나와 object storage gateway를 기동하고, worker consumer·멱등성·재시도·dead-letter 복구·source 재파싱을 실제 서비스 경계에서 검증한다.
5. 모델 미준비, endpoint timeout, OCR 실패, 잘못된 generator citation, 후보 부분 실패, 재시작 복원, worker 장애를 자동화한다.
6. 실측 benchmark와 feedback/retuning, adaptive exploration의 evidence boundary를 검토한다. 탐색 제안은 사용자 확인 없이 확정되지 않아야 한다.

모델 endpoint가 없는 로컬 CI에서는 test double로 동일 계약을 검증한다. 구축된 개발 서버에서는 fallback 결과를 성공으로 간주하지 않는다. 모델 서버 구축 기준은 [`docs/MODEL_RUNTIME_DEPLOYMENT.md`](./docs/MODEL_RUNTIME_DEPLOYMENT.md)를 따른다.

### 0.3a 완료된 초기 계획 — Retrieval Quality Foundation

**스프린트 목표:** “실제 모델을 사용한 문서 업로드·비교 결과를 측정 가능하고 신뢰할 수 있게 만든다.”

#### 작업 순서

1. **후보 준비 상태와 복구 UX를 완성한다.**
   - Backend: 후보별 state/error/attempt를 snapshot과 comparison API에 저장·반환한다.
   - Frontend: `PREPARING`은 대기, `FAILED`는 실패 이유와 job 재시도, `NO_EVIDENCE`는 선택·확정 불가로 표시한다.
   - QA: 새로고침, 취소, 부분 실패, 재시도 뒤 상태 전이를 자동 검증한다.
2. **개발 서버 모델 런타임을 연결하고 end-to-end smoke test를 고정한다.**
   - BGE-M3 embedding, reranker, Tesseract를 기동한다. API는 이미 모델이 존재한다는 전제의 endpoint를 사용하며, `execution-plan`이 단일 진실 공급원이다.
   - 테스트 문서 하나를 업로드해 TEI embedding/rerank/OCR provider 메타데이터까지 확인한다.
3. **golden set과 벤치마크 하네스를 만든다.**
   - 먼저 corpus·질문·정답 근거를 versioned fixture로 고정한다.
   - 그 다음 모델 3종의 recall@k/MRR/latency를 동일 조건에서 저장하고, 모델 추천 화면이 그 결과를 읽도록 연결한다.
4. **대형 문서 비용 제어를 추가한다.**
   - 500 chunks 초과 시 샘플 비교로 전환하고, 확정 후 전체 재인덱싱 job을 생성한다.
   - 샘플 범위와 전체 인덱스 준비 여부를 비교·상세 화면에서 분명히 안내한다.
5. **릴리스 QA를 수행한다.**
   - 모델 미구축/지연, OCR 실패, 후보 일부 실패, 대형 문서, 재방문 복원까지 실제 API 기반 E2E로 확인한다.

#### 이번 스프린트에서 하지 않는 일

- 생성 모델을 붙여 답변을 유창하게 만드는 작업은 다음 스프린트(P1)로 둔다. 그 전에는 retrieval 품질과 근거 상태를 먼저 측정한다.
- 다문서 종합 답변과 Redis/SQS 운영 전환도 이번 범위에서 제외한다. 둘 다 후보 상태 계약과 실제 모델 품질 결과가 안정된 뒤 설계해야 한다.

#### 스프린트 완료 판정

- 실제 모델 provider를 사용한 업로드→비교→확정→검색 smoke test가 성공한다.
- 후보·job·비교 결과가 새로고침 뒤에도 같은 상태와 근거를 보여준다.
- 최소 3개 임베딩 모델의 동일 corpus benchmark 결과가 저장되고 비교 가능하다.
- P0 실패 시나리오가 자동화된 QA에서 재현·복구된다.

모델 서버 구축의 책임·검증 기준은 [`docs/MODEL_RUNTIME_DEPLOYMENT.md`](./docs/MODEL_RUNTIME_DEPLOYMENT.md)를 따른다.

### 0.4 스프린트 실행 기록

| 스프린트 | 상태 | 목표 | 산출물 | 다음 입력 |
| --- | --- | --- | --- | --- |
| Sprint 06 | 완료 | 후보 준비·실패·근거 없음 상태를 실제 job/비교 흐름에 연결 | [`docs/SPRINT_06_CANDIDATE_READINESS.md`](./docs/SPRINT_06_CANDIDATE_READINESS.md), 상태 UX 검토, backend/frontend 회귀 테스트 | 실제 모델 runtime smoke test에서 상태 계약 사용 |
| Sprint 07 | 완료 | golden corpus와 embedding benchmark harness | benchmark API·저장·모델별 비교 화면 | 실제 모델·대표 corpus release gate |
| Sprint 08 | 완료 | 대형 문서 샘플 비교와 확정 뒤 전체 재인덱싱 | chunk 임계값 정책, sample/full job 상태, UI·회귀 테스트 | 실제 대형 파일 E2E |
| Sprint 09 | 완료 | grounded generation·citation 검증·SSE | generator contract, fallback metadata, stream UI·회귀 테스트 | 다문서 retrieval 병합 |
| Sprint 10 | 구현 완료 | 다문서 grounded 검색 | 전역 점수 병합, 다문서 citation, 종합 생성 UI | 실제 모델 smoke와 P1 QA |
| Sprint 11 | 🟡 구현·자동 QA 완료 / 실제 runtime gate 대기 | 실제 모델·실제 파일 release gate | runtime smoke, golden corpus 확대, 실패·복구 E2E | 모델 endpoint가 준비되면 `release_gate.sh` 실행 후 완료 처리 |
| Sprint 12 | ✅ 구현·자동 QA 완료 | 피드백 기반 재튜닝 | explainable recommendation, baseline/outcome artifact, explicit 실행 UI | 실제 benchmark/runtime release gate |
| Sprint 13 | ✅ 구현·자동 QA 완료 | 원본 파일·재현성 | source storage adapter, SHA-256 dedup, provenance, reparse artifact | 실제 object storage smoke |
| Sprint 14 | ✅ 구현·자동 QA 완료 | 운영 job 플랫폼 | durable dispatch receipt/idempotency, heartbeat, retry/backoff, dead-letter/recovery | 실제 Redis/SQS worker smoke |
| Sprint 15 | ✅ 구현·자동 QA 완료 | 적응형 후보 탐색 | exploration ledger, bounded proposals, explicit rollback/restore, no auto-finalize | 실측 evidence·사용성 release review |

Sprint 완료 시에는 항상 이 표, 해당 `docs/SPRINT_XX_*.md`, 자동 검증 기록을 함께 갱신한다. 모델 endpoint가 없는 로컬 CI에서는 network 호출을 mock/test double로 검증하고, 개발 서버에서는 같은 API 계약으로 smoke test를 별도 실행한다.

**Sprint 07–15 통합 QA (2026-08-03):** backend 34 passed / external release gate 2 skipped, frontend 31 passed / format·build 통과. P0 반응형 drawer·재파싱 선택 범위·표시 중단 문구와 P1 대형 문서 exploration sample 경계·SSE preflight·운영 API mock fallback을 보완했다. 상세 근거는 [`docs/SPRINT_07_15_BACKEND_QA.md`](./docs/SPRINT_07_15_BACKEND_QA.md), [`docs/design/SPRINT_07_15_FRONTEND_QA.md`](./docs/design/SPRINT_07_15_FRONTEND_QA.md), [`docs/design/SPRINT_07_15_UX_QA.md`](./docs/design/SPRINT_07_15_UX_QA.md)를 따른다.

---

## 1. 목적과 대상 사용자

RAG를 처음 구축하는 **비전문가**가 (1) 파싱·청킹·임베딩·검색에 대한 기본 판단력을 갖추고, (2) 자신의 문서에 대해 어떤 조합이 적합한지 데이터 기반으로 추천받고, (3) 시간이 지나며 우리 자체 데이터(어떤 임베딩 모델이 우리 문서에 실제로 잘 맞는지)를 축적해가는 것을 돕는 기능 페이지.

---

## 2. 기능 구성 현황

| # | 기능 | 상태 | 위치 |
|---|---|---|---|
| 2.1 | 파싱/청킹/임베딩/검색 비교 가이드 | ✅ 구현 | `apps/frontend/src/pages/GuidePage.tsx` + `RAG_PORTAL_GUIDE.md` |
| 2.2 | 문서별 실제 파싱·청킹·후보 분석 | ✅ 구현 | `apps/backend/app/document_parser.py`, `apps/backend/app/main.py` |
| 2.3 | NotebookLM-inspired 제품 UI·가이드 | ✅ 구현 | `apps/frontend/src/features/rag/`, `skills/notebooklm-inspired-ui-skill/` |
| 2.4 | 문서 타입별 청킹 후보 3개와 검색 페어링 | ✅ 구현 | 적응형 청킹 정책: `docs/SPRINT_05_ADAPTIVE_CHUNKING.md` |
| 2.5 | 임베딩 모델 실측 벤치마크 하네스 | 🟡 기반 구현, 실제 corpus/runtime E2E 대기 | Sprint 07, §0.2 |

---

## 3. 핵심 설계 결정과 근거

이 절은 대화 중 오갔던 "이렇게 할까 저렇게 할까" 논의의 **최종 결론과 왜 그렇게 정했는지**를 기록한다. 나중에 구현하다가 "왜 이렇게 설계했더라?"를 다시 찾아보는 용도.

### 3.1 사용자 플로우: 프로젝트 단위 설정 → 문서 단위 파이프라인 선택

**결정**: 문서를 업로드하면 모든 파서×청커×임베더×검색방식 조합을 다 실행해서 점수를 매기지 않는다. 대신 아래처럼 계층을 나눈다.

**① 프로젝트 설정(질의/questionnaire 기반, 1회, 리소스 절약)**
- **임베딩 모델**: 실제 벤치마크를 사용자에게 매번 돌리게 하지 않고(리소스 제약), 몇 가지 질문(한국어 필요? 온프레미스 필요? 예산?)에 답하면 시스템이 하나를 추천 — 그 추천 로직의 근거 데이터는 §4의 **우리 내부 실측 벤치마크**에서 축적한다. 문서마다 바꾸지 않고 프로젝트(지식베이스) 단위로 고정.
- **GraphRAG 사용 여부**: "질의가 여러 엔티티를 연결해야 하는 경우가 많은가?" 한 가지만 물어서 on/off — 다중 홉 질의가 많으면 켜고, 단순 사실 검색이 대부분이면 끈다(가이드 §4 근거: 단순 사실 검색엔 GraphRAG 이점이 적음).

**② 문서 업로드 시(비교 기반, 문서마다)**
- **파싱+청킹**: 문서 타입(형식, 표 비율, 길이, 구조)에 따라 **청킹 후보 파이프라인 3개**를 룰 기반으로 추천.
- **검색 방식**: 청킹 후보마다 검색 방식을 **전부(N개) 조합하지 않고, 후보별로 2~3개씩만** 페어링(§3.3 참고) — 실제 검색 결과를 나란히 보여줘서 사용자가 고르게 한다.

**왜 이렇게 나눴는가**:
- 임베딩 모델을 문서마다 다르게 쓰면 **벡터 공간이 달라져서 통합 검색 자체가 깨진다**(모델 A로 임베딩한 벡터와 모델 B로 임베딩한 벡터는 같은 유사도 계산으로 비교 불가). 모델별로 인덱스를 분리 운영하는 건 비전문가 대상 포탈엔 과한 인프라 복잡도 — 그래서 프로젝트 단위 고정 + questionnaire로 결정.
- 파싱/청킹/검색 품질은 "그 자체로 좋다/나쁘다"를 점수로 매기기 어렵고, 실제 검색 결과로만 체감된다. 쿼리 없이 "청킹 품질 8점" 같은 점수는 의미가 약하다 — 그래서 이 층은 questionnaire가 아니라 **실제 검색 결과 비교**로 사용자가 고르게 한다.
- 조합을 전부 실행하는 것(파서×청커×임베더×검색방식 풀 그리드)은 비용이 크고, 결과 해석도 어렵다(교란변수 문제, §3.3 참고).

**문서 타입 → 청킹 후보 3개 매핑 예시** (각 후보는 §3.3의 검색방식 2~3개와 페어링됨):

| 문서 특성 | 청킹 후보 3개 |
|---|---|
| 짧고 단일주제 (TXT, 소량 CSV) | ① 청킹 생략 ② 문서단위 청킹 ③ 고정크기 |
| 표 많음 (Excel, 표 위주 PDF) | ① 표 분리 청킹 ② 계층적 청킹 ③ 고정크기(baseline 비교용) |
| 길고 구조적 (정책/매뉴얼 PDF) | ① 구조인식 파싱+계층적 청킹 ② 시맨틱 청킹 ③ 고정크기(baseline) |
| 스캔/이미지형 PDF | ① OCR파서+계층적 청킹 ② OCR파서+시맨틱 청킹 ③ (현재 PDF는 자리표시자 파싱이라 실제 비교 불가 — 경고만 표시) |

판별 기준: `SourceType` + 이미 `RagPortalPage`에서 계산 중인 세그먼트 통계(표 비율, 평균 청크 길이, 문서 길이)를 룰 기반 분기 조건으로 재사용.

### 3.2 표준 벤치마크(MTEB 등)는 우리가 재실행하지 않는다 — 참고만 한다

**결정**: MTEB 같은 표준 벤치마크는 이미 전문 팀이 지속적으로 갱신하며 측정해두고 있으므로, 우리가 같은 걸 다시 돌리지 않는다. 대신:
- **참고용(고정 데이터)**: 리더보드 공개 점수를 그대로 인용(출처·조회일 명시)
- **우리가 직접 측정하는 것**: "이 임베딩 모델이 **우리 실제 문서·질의**에서 얼마나 잘 검색하는가" — 이건 아무도 대신 계산해줄 수 없는 우리만의 데이터

**왜**: 남이 이미 잘 해둔 걸 반복하는 건 시간 낭비이고, 우리의 진짜 컴퓨팅/시간 예산은 "우리 데이터에서의 실측"이라는, 오직 우리만 만들 수 있는 데이터에 써야 한다.

**MTEB v1/v2 라벨링 관련 부기**: 조사 중 2차 출처(블로그)들끼리 어떤 점수가 v1이고 어떤 게 v2인지 서로 엇갈리게 보고하는 것을 발견했다(예: OpenAI `text-embedding-3-large`를 두고 "64.6=v1, v2 집계는 58.96/13위" vs "64.6=v2"로 상반됨). 그래서 가이드 문서에는 단일 표만 유지하고 각주로 이 불확실성을 명시 + [MTEB 공식 리더보드](https://huggingface.co/spaces/mteb/leaderboard) 링크로 대체했다(정적 표로 박제하지 않음 — 리더보드가 Gradio 앱이라 자동 스크래핑도 안 됨을 확인).

### 3.3 임베딩 모델 벤치마크는 파싱/청킹을 고정한 뒤 단계적으로 실험한다 + 청킹×검색방식 페어링 스킴

**결정 (임베딩 실험)**: 임베딩 모델 비교 실험을 할 때 파싱·청킹·임베딩을 동시에 다 바꾸지 않는다. 2단계로 분리한다:
1. **1차**: 파싱·청킹은 **현재 플랫폼이 실제로 만들어내는 `SourceSegment` 청크로 고정** → 임베딩 모델만 바꿔가며 검색 정확도 측정
2. **2차(1차 이후)**: 1차에서 검증된 임베딩 모델로 **고정** → 청킹 방식/크기만 바꿔가며 재측정

**왜**: 여러 변수를 동시에 바꾸면 결과가 달라졌을 때 원인(임베딩 때문인지 청킹 때문인지)을 구분할 수 없는 교란변수 문제가 생긴다. 변수를 하나씩 고정/변경하는 게 과학적으로 유효한 비교이자, 실행 횟수도 조합 폭발보다 훨씬 적다.

**유의점**: 지금 플랫폼의 청킹은 고정 글자수 방식(가이드에서 지적한 이상적이지 않은 방식)이지만, 이걸 1차 실험의 기준선으로 삼는 건 정직한 선택이다 — 실제로 지금 존재하는 방식이고, 임베딩 모델 간 상대적 순위는 청킹이 어느 정도 바뀌어도 크게 뒤집히지 않는 경우가 많아 1차 결론의 유효성은 유지될 가능성이 높다.

**결정 (§3.1의 문서 단위 청킹 후보 비교 시 검색방식 페어링)**: 청킹 후보 3개를 검색 결과로 비교할 때, 검색방식을 완전히 하나로 고정하지도, 전체(N개)와 풀 그리드로 조합하지도 않는다. **청킹 후보마다 검색방식 2개는 고정, 1개는 문서 특성에 따라 가변**으로 페어링한다(GraphRAG는 §3.1에서 이미 프로젝트 단위로 분리했으므로 이 조합엔 포함하지 않음).

| 슬롯 | 방식 | 이유 |
|---|---|---|
| 고정 ① | 하이브리드(BM25+벡터) | dense/sparse 각각의 약점을 서로 보완해 **문서 종류와 무관하게 하한선을 보장**하는 안전한 기본값 |
| 고정 ② | 하이브리드+재순위화 | 여러 벤치마크에서 하이브리드 대비 손해 없이 정밀도를 추가로 올리는 것으로 일관되게 보고됨(가이드 §4) — 이것도 문서 종류 무관하게 안전 |
| 가변 ③ | 표/코드/ID 등 정확 일치 중요 문서 → BM25 단독과 비교 / 서술형·의미중심 문서 → dense 단독과 비교 | 고정 ①·②가 "보장된 성능"이라면, 가변 ③은 반대로 **"이 문서엔 굳이 하이브리드까지 안 가고 더 단순한 방식만으로 충분한가?"를 검증하는 대조군(baseline)** |

→ 청킹 3개 × 검색방식 2~3개 ≈ **총 6~9개 검색 결과 세트**. "청킹 후보 3개 탭 → 탭마다 검색방식 하위 비교" UI 구조로 비전문가도 훑어볼 수 있는 양으로 관리.

**청킹×검색방식 상호작용 효과에 대한 주의**: 청킹 방식과 검색 방식은 실제로 상호작용한다 — 예를 들어 BM25는 원문 구조/용어가 보존된 청크에 유리하고, dense는 의미 단위로 잘 나뉜 청크(시맨틱 청킹)에 유리해서 "하이브리드에서 1등인 청킹"이 "순수 dense에서도 1등"이라는 보장은 없다. 다만 이게 설계 결함은 아닌데, 우리 목표가 "청킹 방식의 절대적 우수성 증명"이 아니라 **"실제로 배포할 그 검색 설정 하에서 뭐가 제일 잘 되는가"**이기 때문 — 고정된 배포 조건 그대로 테스트하는 것은 정확한 방법론이다. 진짜 주의할 지점은 **나중에 프로젝트의 검색 설정 자체가 바뀌면(예: GraphRAG를 새로 켜면) 그 전에 검증된 청킹 추천이 재검증 없이는 더 이상 최적이 아닐 수 있다**는 것 — 그래서 청킹 벤치마크 결과를 저장할 때 **"어떤 검색 설정 하에서 테스트됐는지"를 메타데이터로 같이 기록**(`retrieval_config: "hybrid+rerank"` 등)해두고, 프로젝트가 검색 설정을 바꾸면 "이 청킹 추천은 재검증이 필요합니다" 경고를 띄우는 것을 구현 시 반영한다.

### 3.4 무료 임베딩 모델은 로컬이 아니라 Hugging Face Inference API로 테스트한다

**결정**: 로컬 GPU가 없는 개발 환경(맥, CPU 12코어) 제약을 우회하기 위해, 후보 임베딩 모델을 로컬 다운로드/추론이 아니라 **Hugging Face Inference API(서버리스, HF의 GPU에서 실행)** 로 호출한다.

**왜**: 로컬 CPU로는 큰 모델(Qwen3-Embedding-8B 등)이 비현실적으로 느리다. HF API를 쓰면 로컬 연산 제약 없이 더 넓은 모델 범위(작은 모델부터 MTEB 1위급 모델까지)를 동일한 조건으로 비교할 수 있다.

**제약사항(구현 시 고려)**:
- 무료 티어는 rate limit 있음 — 벤치마크 규모(질의 수십~수백 건)엔 충분하나 대량 배치엔 부적합
- 모든 모델이 상시 서버리스로 떠있지 않아 콜드스타트/일시적 503 가능(재시도 로직 필요)
- **API 토큰(`HF_TOKEN`) 필요** — `.env`에 저장, 절대 커밋 금지. `app/core/config.py`의 `Settings`에 기존 `llm_api_key` 패턴과 동일하게 `huggingface_api_key` 필드 추가 예정.

**대상 모델 (전부 무료/오픈소스, HF API로 호출)**:
| 모델 | 파라미터 | 라이선스 | 비고 |
|---|---|---|---|
| BGE-M3 | 568M | MIT | dense+sparse+multi-vector 통합, 한국어 포함 다국어 |
| Qwen3-Embedding-0.6B | 0.6B | Apache 2.0 | 경량, 한국어 명시 지원 |
| Qwen3-Embedding-8B | 8B | Apache 2.0 | 가이드 상 MTEB 최고 점수권, HF API 덕분에 후보 가능 |
| EmbeddingGemma-300M | 300M | — | 초경량(RAM 200MB 미만 구동 가능) |
| Nomic-embed-v2 | 137M | — | 가장 가벼운 후보 |

### 3.5 파싱은 자동 트리거로 전환, 청킹 후보 추천은 파싱 전/후 2단계로 나눈다

**현재 상태 확인**: 코드 조사 결과 파싱은 **수동 단계**다 — 업로드(`SourceManagerPage.tsx` → `useUploadSource`)와 파싱(`DocumentChunkViewerPage.tsx`의 "Parse source" 버튼 → `useParseSource`)이 분리되어 있고, 업로드 직후엔 `SourceData.status = UPLOADED`일 뿐 `SourceSegment`(청크)는 사용자가 버튼을 눌러야 생성된다(`apps/backend/app/modules/source/router.py`의 `upload_source`/`parse_source`).

**결정**:
1. **파싱을 자동 트리거로 전환**한다 — 업로드 직후(또는 RAG 포탈에서 해당 문서를 처음 열람하는 시점)에 `parse_source`를 자동 호출해, 비전문가가 "왜 청크 데이터가 안 보이지?"를 겪지 않게 한다. (기존 수동 "Parse source" 버튼은 재파싱 용도로 남겨둘 수 있음.)
2. **청킹 후보 추천을 파싱 전/후 2단계로 나눈다**:
   - **1차(파싱 전, 즉시)**: `SourceType`+파일 크기 등 메타데이터만으로 대략적인 후보를 먼저 보여준다(예: "Excel이니 표 위주일 가능성 높음 → 이 3개 후보").
   - **2차(파싱 후, 정밀)**: 실제 `SourceSegment` 구조 통계(표 비율, 평균 청크 길이 등 — `RagPortalPage.tsx`의 `recommendationFor()`가 이미 계산 중)로 후보를 재계산/확정한다.

**왜**: 표 비율 같은 진짜 구조 통계는 파싱이 끝나야만 나오기 때문에, 청킹 후보 추천의 정밀도가 파싱 여부에 좌우된다. 자동 트리거로 그 지연을 사용자에게 노출하지 않고, 그 사이엔 메타데이터 기반의 거친 1차 추천으로 공백을 메운다.

---

## 4. 임베딩 모델 벤치마크 하네스 — 구현 계획 (미착수)

### 4.1 테스트 코퍼스 전략

- **지금**: 실제 플랫폼 문서(`SourceSegment`)를 코퍼스로 사용. 대표 질의는 직접 작성(정답 청크를 수동 지정) — 소규모(질의 수십 건)로 시작.
- **나중에**: 사용자 확인상 "실제 문서도 나중엔 사용해야 할 것 같고, 표준 벤치마크 데이터셋도 사용하고 싶다"고 하여, 표준 다국어/한국어 검색 데이터셋(예: MIRACL 한국어 서브셋)을 **보조 코퍼스**로 추가하는 것도 스코프에 남아있음 — 단, §3.2 원칙(표준 벤치마크의 "일반 순위"는 리더보드 참고로 충분하므로, 표준 데이터셋을 쓰더라도 목적은 "우리 조건에서 재현되는지" 검증이지 리더보드 재생산이 아님)을 유지.

### 4.2 실행 방식 (초안)

1. 코퍼스(우리 문서 청크) + 질의-정답 쌍 준비
2. 각 후보 임베딩 모델로 코퍼스 전체를 HF Inference API 호출해 임베딩
3. 각 질의를 동일 모델로 임베딩 → 코사인 유사도로 top-k 검색
4. 정답 청크가 top-k 안에 있는지로 recall@k 계산(+ 응답 지연시간, 벡터 차원당 저장 비용 등 부가 지표)
5. 결과를 DB에 적재

### 4.3 저장 스키마 (초안, 마이그레이션 전)

```
EmbeddingBenchmarkRun
  id, project_id, corpus_description, query_count, created_at

EmbeddingBenchmarkResult
  id, run_id (FK), model_name, model_provider("huggingface"),
  recall_at_5, recall_at_10, avg_latency_ms, dimension,
  is_reference_score(bool, MTEB 리더보드 인용값인지 실측값인지 구분),
  source_url(참고 점수일 경우 출처), measured_at
```

- `is_reference_score`로 "우리가 실측한 값"과 "MTEB 리더보드에서 인용한 참고값"을 같은 테이블에서 구분해 나란히 비교 가능하게 한다(§3.2 설계와 일치).

### 4.4 페이지 노출

`RagPortalPage.tsx`의 "3. 임베딩 모델" 섹션에 신규 서브섹션 추가 — "우리 데이터 실측 결과" 표(모델/recall@k/지연시간) + "참고: 공개 벤치마크 순위" 표를 나란히 배치. 반복 실행할 때마다 새 `EmbeddingBenchmarkRun`이 쌓여 추이를 볼 수 있게 한다.

---



## 5. RAG 인스턴스 생성 플로우 — 신규 페이지 스펙

`RagPortalPage`(가이드/문서분석, §2.1~2.3, 완료)와는 별개로, **"내가 만든 RAG들을 관리하고 새로 만드는"** 신규 기능 페이지 세트. 사용자가 정리한 15개 항목을 단계별 스펙으로 재구성.

### 5.1 화면/라우트 구성

| 화면 | 역할 | 비고 |
|---|---|---|
| **RAG 대시보드** (`rag-dashboard`) | 내가 만든 RAG 인스턴스 목록 | `ProjectListPage`류의 리스트+"새로 만들기" 패턴 재사용 가능 |
| **RAG 생성 화면** (신규) | 임베딩 선택 → 문서 업로드 → 파이프라인 튜닝(채팅형)까지 **한 화면 이탈 없이** 진행 | 아래 §5.2 |
| **RAG 상세 화면** (신규, 대시보드에서 항목 클릭 시 진입) | 완성된 RAG 인스턴스 하나를 관리 — ① 설정 스펙 조회, ② 채팅형 검색(실사용), ③ 문서 추가/삭제 | 아래 §5.2a — §3(혼동 방지) 원칙에 따라 3개 영역을 화면 안에서도 구분(탭/섹션)해야 함 |

### 5.2 생성 플로우 단계

```
[RAG 대시보드] --새로 만들기--> [생성 화면 진입]
                                      │
                                      ▼
                          1) 질의(questionnaire)로 임베딩 모델 결정
                                      │  ※ 이 시점에 목록에는 이미 RAG 항목이 "생성됨" 상태로 표시
                                      ▼
                          2) (화면 이탈 없이) 문서 업로드
                                      │
                                      ▼
                          3) 업로드 즉시: 자동 파싱 → 청킹 후보 계산 → 검색 조합 계산
                             → 각 조합으로 실제 청킹까지 수행해 임시 저장
                                      │
                                      ▼
                          4) 채팅형 질의 입력 → 각 (청킹×검색) 조합별 결과를 나란히 제시
                             ※ 비교 대상은 "검색된 원본 청크"가 아니라 그 청크를 근거로 LLM이 생성한 답변(기본 뷰)
                               — 화면 안 토글로 "원본 조각만 보기" 뷰로 전환 가능. 아래 5.2c 참고
                                      │
                                      ▼
                          5) 사용자가 결과 중 1개 이상을 선택 (복수 선택 가능 — 선택된 조합 각각의 "선택 횟수"를 누적)
                                      │
                                      ▼
                     4~5 반복 (사용자가 원하는 만큼, 매 반복마다 지금까지의 선택 횟수를 보여주며 진행)
                                      │
                                      ▼
                     ※ 1위 조합이 동률이면 완료 처리 불가 — 단독 1위가 나올 때까지 4~5 강제 반복
                                      │
                                      ▼
                          6) 단독 1위(동률 없음)가 확정되면 "누적 선택 횟수가 가장 많은 조합"으로 확정
                                      │
                                      ▼
                     확정된 청킹+검색 조합을 문서 메타데이터에 기록 → RAG 인스턴스 완성 상태로 전환
                                      │
                                      ▼
                            [RAG 상세 화면]으로 진입 (§5.2a)
```

### 5.2c 비교 화면에서 무엇을 보여줄 것인가 (답변 vs 원본 조각)

4단계(채팅형 비교)에서 사용자가 실제로 무엇을 보고 "더 낫다"를 판단할지에 대한 결정. 두 극단의 트레이드오프:

- **원본 청크(검색 결과)만 보여주기**: 실제로 튜닝 대상인 리트리벌 품질을 직접 보는 것이라 정확하지만, 비전문가에게는 문맥 없는 파편이라 좋고 나쁨을 판단하기 어렵다.
- **LLM 생성 답변만 보여주기**: 판단은 쉽지만, LLM이 부족한 리트리벌 결과도 그럴싸하게 다듬어버릴 위험이 있다 — 관련 청크를 놓쳤는데도 매끄러운 답변을 만들면 사용자가 리트리벌이 나빴다는 걸 못 알아채고, 결과적으로 자연어 유창함에만 시선이 쏠려 정작 비교하려는 대상(리트리벌 품질)이 흐려진다.

**절충안**: 기본 뷰는 **LLM 생성 답변**으로 하되(비전문가가 편하게 판단), 그 답변 아래에 **근거로 쓰인 원본 청크(레퍼런스)를 함께 노출**한다. 다만 답변에만 매몰되는 걸 막기 위해, 별도 설정이 아니라 **같은 화면 안의 뷰 전환(토글/탭)** 으로 "원본 조각만 보기" 모드를 제공한다 — 평소엔 답변 뷰로 편하게 고르고, 애매하거나 궁금할 때는 원본 조각 뷰로 바로 확인 가능. §5.2b의 "순화된 이름 + '?' 아이콘 호버" 패턴과 같은 결(기본은 쉬운 표현, 원하면 상세 확인)로 통일한다.

**출처는 답변 아래가 아니라 답변 문장 안에 인라인으로 표시**: 근거 청크를 답변과 분리된 별도 영역에 나열하는 대신, 답변 문장 중간에 "…80~150달러입니다[제5조 참고]." 처럼 **인라인 각주/배지**로 바로 연결한다 — 어느 문장이 어느 근거에서 나왔는지 훑어보기 쉬워지고, 클릭/탭하면 해당 원본 청크가 펼쳐지는 식으로 §5.2c의 "원본 조각 보기"와 자연스럽게 연결된다.

**답변 생성은 조합별로 동시에 스트리밍**: §5.6의 스트리밍 원칙이 이 화면에도 적용되며, 나란히 놓인 각 조합의 답변은 **독립적으로 동시에 토큰 단위 스트리밍**된다 — 먼저 끝난 조합은 먼저 완성된 채로 보이고 나머지는 계속 흘러나오는 형태(§5.6의 "완료된 조합부터 순차 공개" 원칙과 동일 선상).

**투표 시 참고 정보로 응답 소요 시간 노출**: 각 조합의 답변 카드에 "(조합명, 답변 완료까지 걸린 시간)"을 부가 정보로 함께 보여준다 — 내용 품질이 비슷해 보일 때 사용자가 속도까지 고려해 선택할 수 있게 하기 위함. 다만 이 시간 정보가 선택을 강제하거나 눈에 띄게 강조되면 "빠른 게 무조건 좋다"는 오해를 줄 수 있으므로, 답변 내용보다는 작고 부차적인 표기(예: 카드 하단 작은 글씨)로 둔다.

### 5.2d 실사용 시나리오 예시 (이해를 돕기 위한 가상 예시)

아래는 실제 구현 스펙이 아니라, 지금까지의 흐름(§5.2~§5.2c)이 실제로 어떻게 체감되는지 보여주기 위한 예시 시나리오다.

**예시 문서**: 사내 문서 `출장비_지급_규정.pdf` (텍스트 일부)

```
제3조 (국내 출장 식비) 국내 출장 시 1일 3식 기준 5만원을 한도로 실비 지급한다.
제5조 (해외 출장 식비) 해외 출장 시 국가 등급(A/B/C)에 따라 1일 80~150달러를
  한도로 지급하며, 항공료는 이코노미 클래스 기준 실비로 정산한다.
제7조 (숙박비) 국내 출장 숙박비는 1박 10만원을 한도로 하며, 해외 출장 숙박비는
  국가 등급별 별도 기준(붙임 2 참조)을 따른다.
```

**1단계(임베딩 질의)**: "주로 어떤 언어의 문서인가요?" → 한국어 / "온프레미스가 꼭 필요한가요?" → 아니오 → BGE-M3 추천·선택.

**2~3단계**: 위 PDF 업로드 → 자동 파싱 → 청킹 후보 3개 계산(예: 조항 단위, 문단 단위, 512자 고정 길이) → 각 후보로 임시 청킹·인덱싱.

**4단계(비교 질의 예시 1)**: 사용자가 "해외 출장 갈 때 하루 식비 한도가 얼마야?"라고 입력.

| 조합 | 표시되는 답변(기본 뷰) | 근거 청크(펼쳐보기) |
|---|---|---|
| 조항 단위 청킹 + 하이브리드 검색 | "해외 출장 시 국가 등급(A/B/C)에 따라 1일 80~150달러입니다." | 제5조 전문(등급/한도/항공료 정보가 한 청크에 온전히 보존됨) |
| 512자 고정 길이 청킹 + BM25 | "출장비는 지급 규정에 따라 다릅니다." | 제5조가 청크 경계에서 잘려 "80~150달러" 부분이 다른 청크로 분리 → 핵심 수치 누락 |

→ 사용자는 (자연어만 보고도) 첫 번째 답변이 더 구체적이고 도움이 된다고 판단해 **조항 단위+하이브리드**를 선택. 이때 "왜 두 번째가 부실한지" 궁금하면 근거 청크를 펼쳐 "정보가 잘렸다"는 걸 직접 확인할 수 있음(§5.2c).

**4단계(비교 질의 예시 2, 반복 라운드)**: "국내 출장 숙박비는 얼마까지 돼?" → 이번에도 조항 단위 조합이 "1박 10만원" 정확히 답하고, 다른 조합은 국내/해외 조건을 섞어 답함 → 다시 조항 단위 선택 → 누적 선택 횟수에서 조항 단위+하이브리드가 단독 1위 → 확정.

**결과**: 이 문서는 "조항 단위 청킹 + 하이브리드 검색" 조합으로 확정되어 RAG 상세 화면(§5.2a)에 기록되고, 이후 실사용 채팅에서 이 조합으로 질의응답이 이뤄진다.

### 5.2e 신뢰 확보를 위한 추가 원칙

튜닝 화면(§5.2)과 실사용 화면(§5.2a) 모두에 공통으로 적용되는, 사용자가 답변을 믿고 쓸 수 있게 하기 위한 원칙.

- **근거가 부족하면 "모른다"고 답한다(hallucination 방지)**: 검색된 청크의 관련도(유사도 점수 등)가 일정 기준 이하이면, LLM이 억지로 그럴싸한 답을 만들어내지 않고 **"관련 문서를 찾지 못했습니다"** 류의 답을 하도록 프롬프트/후처리 단계에서 강제한다 — 비전문가는 답이 틀렸는지 스스로 검증하기 어렵기 때문에, 근거 없는 답을 내놓지 않는 것이 신뢰 확보의 최우선 원칙이다. 튜닝 비교(§5.2) 중에도 마찬가지로 적용되어, "이 조합은 아예 못 찾음"이라는 결과 자체가 비교 대상에 포함될 수 있다.
  - **판단 기준은 리트리벌 방식마다 다르다**: 벡터 검색은 유사도 점수가 잘 정규화돼 있어 임계값을 걸기 쉽지만, BM25는 점수 스케일이 코퍼스/질의마다 달라 원점수 그대로 임계값을 걸기 어렵고, 재순위화(rerank) 점수는 비교적 안정적으로 정규화돼 있어 임계값 적용에 더 적합하다 — 즉 조합에 재순위화가 포함돼 있으면 그 점수를, 아니면 벡터 유사도를 기준으로 삼는 식으로 **내부적으로 방식별 판단 로직을 분리**한다.
  - **사용자에게는 숫자 임계값이 아니라 "검색 민감도" 3단계로 노출**: 실제 임계값 수치는 비전문가에게 아무 의미가 없으므로 노출하지 않고, 대신 (선택 기능으로) **"유연하게 / 평이하게 / 엄격하게"** 3단계 정성적 옵션만 제공한다 — "평이하게"를 기본값으로 하고, 내부적으로는 이 3단계를 리트리벌 방식별(벡터/rerank 등, 위 항목) 적절한 점수 기준으로 매핑한다. §5.2b의 "순화된 이름 + '?' 아이콘" 패턴과 동일한 결로, 각 단계 옆 "?" 호버로 "유연하게=더 많은 결과를 보여주되 관련성이 낮을 수 있음, 엄격하게=확실한 근거만" 식의 설명을 덧붙인다.
  - **이 3단계 선택은 실사용(§5.2a) 단계에서만 노출하고, 튜닝 비교(§5.2 4단계) 중에는 "평이하게"로 고정**: 문서 업로드 후 조합을 비교하는 단계는 청킹×검색 "조합" 자체를 비교하는 게 목적이라, 여기에 민감도 옵션까지 얹으면 변수가 늘어나 비교가 흐려진다. 따라서 튜닝 중에는 민감도를 노출하지 않고 항상 "평이하게" 기준으로만 비교하며, 확정 후 실사용 화면에서만 사용자가 필요시 민감도를 조정할 수 있게 한다.
- **실사용 중 피드백(👍/👎)은 선택 기능으로 제공**: 확정 후 실사용(§5.2a) 화면에서도 답변마다 간단한 피드백 버튼을 둘 수 있으나, **필수 응답은 아니다** — 사용자가 원할 때만 남기는 부가 기능. 다만 부정 피드백이 일정량 쌓이면(예: 특정 문서/조합 기준) "재튜닝을 권장합니다" 같은 알림을 대시보드에 노출하는 용도로 활용한다. (구체적인 임계값·알림 문구는 §6 미결.)

### 5.2a RAG 상세 화면

대시보드에서 완성된(또는 문서를 하나 이상 확정한) RAG 인스턴스를 클릭하면 진입. 한 화면 안에 3개 영역을 **구분되게** 배치(§3 혼동 방지 원칙 — 탭 또는 명확히 분리된 섹션):

1. **설정 스펙 조회**: 이 RAG의 임베딩 모델, GraphRAG 사용 여부, 첨부된 문서 목록과 각 문서별로 확정된 청킹+검색 조합(§3.3의 `retrieval_config` 메타데이터)을 읽기 전용으로 표시.
2. **검색(채팅) — 실사용 모드**: 확정된 조합으로 실제 질의응답. `RagAnswerWorkspacePage`와 동일한 채팅 UI를 쓰되, 여기서는 조합 비교가 아니라 **이미 확정된 조합으로만** 동작 — §5.2의 튜닝 모드 채팅과 시각적으로 구분(예: 헤더 카피 "검색"만 있고 "여러 조합 비교" UI 요소 없음).
   - **검색 대상 범위 선택**: 검색 전에 "전체 문서" 또는 "특정 문서 선택"을 고를 수 있어야 한다(다중 선택). 선택된 문서 범위에 따라 검색 방식 셀렉트 노출 여부가 달라짐 — §5.2b 참고.
3. **문서 관리(추가/삭제)**:
   - **추가(2개 이상의 문서가 쌓이는 실무 시나리오 반영)**: 실제 운영에서는 비슷한 성향의 문서를 하나의 RAG로 묶어 계속 추가하는 경우가 많다는 점을 반영해, 새 문서를 추가할 때 **먼저 사용자에게 선택지를 준다**:
     - **(a) 기존에 확정된 조합을 그대로 적용** — 이미 다른 문서로 검증된 청킹+검색 조합을 새 문서에도 그대로 씀. 튜닝 단계를 건너뛰고 즉시 파싱+청킹만 수행.
     - **(b) 이 문서는 따로 튜닝** — §5.2의 3~9단계(자동 파싱 → 후보 계산 → 채팅형 비교 → 확정)를 새 문서 기준으로 다시 거침. 어느 경우든 **임베딩 모델은 인스턴스 단위로 고정**되어 재질의하지 않음. **UI는 §5.2 생성 플로우의 튜닝 화면(컴포넌트)을 그대로 재사용**하고 진입점만 다르게 한다(모달 등 축소 UI는 만들지 않음) — 이 흐름 자체가 다단계·비교 화면이라 모달엔 부적합하고, 하나의 컴포넌트로 일관된 사용자 경험을 주는 게 낫다는 판단. 단, 1단계(임베딩 질의)는 건너뛰고 **3단계(자동 파싱+후보 계산)부터 바로 진입**한다.
     - 어느 경로를 택하든 **문서마다 청킹+검색 조합 메타데이터는 항상 기록**한다(같은 조합을 재사용했어도 "이 문서는 이 조합"이라는 기록 자체는 남김) — §5.2b의 이종 조합 검색 처리가 이 기록에 의존.
   - **삭제**: 문서를 제거하면 해당 문서에 대해 확정돼 있던 청킹+검색 조합 데이터도 함께 제거(cascade).
   - 이 영역은 "검색(실사용)" 영역과는 별도 섹션으로 둬서, 문서를 추가/삭제하는 관리 작업과 실제 질의응답 작업이 화면상 뒤섞이지 않게 한다.

### 5.2b 검색 시 문서 간 조합 이종성(異種性) 처리

문서 추가 시 (a)재사용 / (b)별도 튜닝을 선택할 수 있게 되면서, 한 RAG 인스턴스 안에 **서로 다른 청킹+검색 조합을 가진 문서가 섞일 수 있다.** 이때 실제 검색(§5.2a의 2번, 실사용 채팅)이 어떤 조합을 적용할지 결정해야 한다.

- **청킹은 문서별 구조적 속성**(이미 그 방식으로 쪼개져 저장됨)이라 문서마다 달라도 검색 자체엔 문제없음 — 청크가 무슨 방식으로 나뉘었든 인덱스에는 그냥 청크로서 존재.
- **검색 방식(하이브리드/하이브리드+rerank/가변 슬롯)은 질의 시점의 파라미터**이지 문서마다 물리적으로 분리된 인덱스가 필요한 게 아니므로, 다음과 같이 단순하게 처리 가능. 판단은 **RAG 전체가 아니라 검색 대상 범위로 선택된 문서들 기준**(§5.2a)으로 한다:
  - **선택된 문서들의 확정 조합이 모두 동일** → 검색 UI에 아무 것도 노출하지 않고 그 조합을 그대로 적용(지금까지 설계와 동일, 가장 흔한 경우일 것으로 예상 — 비슷한 문서를 한 RAG로 묶는다는 전제 하에).
  - **선택된 문서들의 확정 조합이 서로 다름** → 검색 화면에 **셀렉트(드롭다운)** 를 노출하되, **§3.3의 고정 2개 중 하나(하이브리드 — 가장 안전한 기본값)를 기본 선택값으로 미리 채워둔다.** 나머지 후보(고정 ②인 하이브리드+rerank, 그리고 선택된 문서들이 실제로 갖고 있는 가변 슬롯 조합들)는 필요할 때 사용자가 바꿔 선택할 수 있게 셀렉트 옵션으로 제공한다 — 즉 "이론상 가능한 전체 목록"이 아니라 **선택된 문서 범위 안에 실제로 존재하는 조합들만** 셀렉트에 노출하되, 그중 일반적인 것을 기본값으로 미리 골라준다.
- **라벨링**: 셀렉트/UI 어디에서든 "하이브리드+재순위화" 같은 기술 용어를 그대로 노출하지 않고 **순화된 이름**(예: "일반 검색", "정밀 검색" 등, 정확한 문구는 구현 시 확정)을 쓴다. 대신 옆에 **"?" 아이콘**을 두고 호버(hover) 시 원래 기술 용어와 자세한 설명을 보여준다 — 비전문가는 순화된 이름만 보고 고를 수 있고, 궁금하면 더 깊게 알아볼 수 있는 구조.
- **조합 종류가 많아지는 경우의 clutter 우려는 기각**: 문서가 늘어 조합 종류가 3~4개 이상으로 늘어나더라도, 어차피 **셀렉트(드롭다운) 컴포넌트라 화면에 다 펼쳐지는 게 아니라 클릭해야 열리는 형태**이므로 별도의 그룹핑/접기 UI 없이도 지저분해지지 않는다고 판단 — 별도 처리 불필요.

### 5.3 핵심 설계 포인트 (사용자 정리 항목 반영)

- **임베딩 결정 시점에 이미 RAG가 "생성"된다**: 대시보드 목록에는 문서 업로드 전이라도 즉시 나타남(예: "설정 중" 상태) — 화면을 나가도 이어서 할 수 있어야 함을 시사.
- **조합 비교는 "임시 저장" 상태**: 최종 확정 전까지 각 청킹×검색 조합의 청킹 결과는 임시로 저장해두고, 확정되지 않은 조합은 이후 정리(폐기) 대상.
- **비교는 문서 일부가 아니라 업로드한 문서 전체를 대상으로(단, 문서 크기에 따라 적응)**: 완성형 제품을 목표로 하므로 기본은 문서 전체로 비교하되, 문서가 크면 튜닝(비교) 단계만 샘플로 빠르게 하고 최종 확정 후 문서 전체로 재인덱싱한다. 판단 기준은 파일 크기가 아니라 **청킹 후보로 나눴을 때 예상되는 청크 개수**(실제 재임베딩 비용을 좌우하는 변수 — 검색방식은 질의 시점 파라미터라 재임베딩과 무관하므로, 비용은 "청킹 후보 3개 × 청크 개수"이지 6~9개 조합 전체에 비례하지 않음). **잠정 출발값: 청크 개수 약 500개 이하 → 문서 전체 비교, 초과 → 샘플링** — 이 500이라는 수치는 실측 없이 잡은 가설값이며, §6에 남긴 "청킹 후보 하나당 실제 처리 시간" 벤치마크로 확정 필요. 하드코딩하지 않고 조정 가능한 설정값으로 둔다.
- **선택은 복수 선택 가능**: 채팅형 비교 라운드마다 사용자는 결과 중 **1개 이상을 선택**할 수 있다(여러 조합이 동일하게 만족스러울 수 있으므로 억지로 하나만 고르게 강제하지 않음) — 선택된 조합 각각의 선택 횟수가 함께 누적된다.
- **선택 결과 메타데이터화**: 확정된 조합(청킹 방식+검색 방식, §3.3의 `retrieval_config` 개념과 동일 선상)을 문서/RAG 인스턴스에 기록해 "이 문서는 어떤 조합으로 만들어졌는지" 나중에 조회 가능해야 함.
- **비전문가 대상 기본값 + 향후 확장 여지**: 세부 파라미터(청크 크기, top-k 등)는 지금은 일반적인 기본값을 적용해 사용자에게 노출하지 않지만, 향후 버전에서는 4~5번 반복 선택 결과에 따라 **후보군이 점점 좁혀지고 파라미터에도 미세 변주를 주는 방식**(bandit/적응형 탐색과 유사한 방향)으로 진화 가능하도록 설계 여지를 남긴다.
- **확장성(배열 기반 입력)**: 지금은 "문서 1개씩 업로드"로 개발하되, 백엔드/프론트 인터페이스는 문서를 **배열**로 받도록 설계해(예: `document_ids: string[]`) 나중에 다중 문서 업로드로 자연스럽게 확장 가능하게 한다.
- **"생성/튜닝"과 "사용"의 명확한 분리**: RAG 생성 화면의 채팅형 비교 UI와, 완성된 RAG로 실제 질의응답하는 화면(`RagAnswerWorkspacePage`)은 **서로 다른 화면/모드**로 명확히 구분한다 — 같은 채팅 UI를 재사용하더라도 "지금 이게 튜닝 모드인지 실사용 모드인지" 헷갈리지 않게 시각적으로 구분(예: 배지, 다른 헤더 카피).
- **동점 처리**: 최다 선택 조합이 동률이면 **완료 처리를 허용하지 않는다** — "다음 라운드 진행"은 사용자가 원할 때 언제든 누를 수 있지만, "완료" 버튼은 동률인 동안 **비활성화**되어 단독 1위가 나오기 전에는 확정할 수 없다(§5.7(B) 참고, 강제로 라운드를 돌리는 게 아니라 완료 액션 자체를 막는 방식). "적당히 동률로 끝내기"를 허용하지 않음으로써 이 문서에 대한 조합 선택은 항상 명확한 하나로 귀결된다.
- **미완성 인스턴스 정리 정책**: 임베딩 모델은 정했지만 문서를 업로드해 청킹+검색 조합 선택까지 끝내지 못한 RAG 인스턴스는, 재방문 시 **첨부했던 원본 문서(`SourceData`)만 남기고 나머지(임시 저장된 청킹 결과, 조합별 선택 횟수 등)는 전부 버리고 처음부터(3단계: 자동 파싱+후보 재계산)부터 다시 시작**한다 — 중간 상태를 이어서 복원하려 하지 않음으로써 "어중간하게 튜닝되다 만" 상태가 남지 않게 한다.
- **확정 후 미확정 조합 정리**: 단독 1위 조합이 확정되면, 그 시점에 **나머지(선택되지 않은) 조합들의 임시 청킹 결과는 삭제 처리**한다 — 확정된 조합의 데이터만 영구 보관하고, 비교용으로만 쓰였던 나머지는 남겨두지 않는다.

### 5.4 UX 참고 레퍼런스 (딥리서치로 구체화, §5.7에서 화면 구조로 반영)

웹 리서치(WebSearch/WebFetch)로 확인한 실제 제품 UI 패턴. 2차 출처(블로그/티어다운) 기반 항목은 "미검증"으로 표시.

- **NotebookLM**: 좌측 **소스(문서) 사이드바**(체크박스로 컨텍스트 포함/제외 토글) + 중앙 **채팅** + 우측 **Studio**(파생 산출물) 3분할 레이아웃. 채팅 답변의 인용은 **번호가 매겨진 클릭 가능한 칩**으로 표시되며, 클릭 시 사이드바가 해당 근거 위치로 스크롤됨. → §5.7 상세 화면 레이아웃의 기반.
- **Hugging Face Hub**: 목록(모델/데이터셋 카드 그리드 — 이름·태그·지표를 카드에 압축 표시)과 상세(본문+메타데이터 사이드바 분리) 패턴. Inference Playground의 모델 비교 UI는 존재가 확인됐으나 레이아웃 세부사항은 검색으로 확인하지 못함(**미검증**). → §5.7 대시보드 카드 디자인의 기반.
- **Open WebUI**: 표준 채팅 UI(좌측 사이드바=대화이력, 상단=모델 선택) 외에 **"Compare" 토글로 진입하는 다중 모델 비교 모드**가 실제로 존재 — 모델 여러 개를 다중 선택하면 하나의 질의에 대해 **여러 응답이 나란히(또는 좁은 화면에서는 탭으로) 동시에 스트리밍**되는 `MultiResponseMessages` 구조. → §5.2/§5.7의 조합 비교 화면과 가장 직접적으로 맞닿는 레퍼런스.
- **Chatbot Arena (LMSYS)**: 2열로 응답을 동시 스트리밍하고, 완료 후 하단 **투표 바**(A가 낫다/B가 낫다/무승부/둘 다 별로)로 평가받는 구조. 다만 Arena는 투표 전까지 모델 정체를 숨기는 "블라인드" 방식인데, 우리는 이미 §5.2b에서 조합에 순화된 라벨을 **처음부터 보여주기로** 결정했으므로 블라인드 방식은 채택하지 않음 — 대신 "무승부/둘 다 별로" 같은 **강제 이분법이 아닌 투표 옵션**만 참고해 채택(이미 §5.3에서 "1개 이상 선택 가능"으로 유사하게 결정돼 있음).
- **Perplexity.ai**: 답변 문장 끝에 붙는 **작은 번호 칩** → 호버 시 출처 미리보기(도메인/제목/발췌) → 클릭 시 전체 출처 목록으로 이동, 하는 **3단계 점진적 노출**(미검증: 2차 출처인 UX 티어다운 기반). → §5.2c의 "인라인 출처 표시" 결정을 구체화하는 근거.
- **GitHub Actions 실행 화면**: 목록에서는 **행(row)별 상태 아이콘**(대기/진행중/성공/실패)만 보이고, 클릭하면 단계별(step) 상태와 로그를 볼 수 있는 상세 화면으로 이동 — 다른 화면으로 이동해도 상태가 유지·재조회됨. 다만 "N개 중 M개 완료" 같은 분수 형태 진행률은 GitHub Actions 자체에서 확인되지 않음(계단식 상태만 존재, **미검증 갭**) → §5.6의 폴링 기반 상태 배지에 참고하되, 분수형 진행률 표시는 우리가 별도로 설계해야 함.

### 5.5 데이터 모델에 미치는 영향 (초안 수준)

이 플로우를 구현하려면 "RAG 인스턴스"라는 개념 자체가 신규 엔티티로 필요해 보인다(현재 플랫폼엔 프로젝트 하위에 이런 단위가 없음):

```
RagInstance
  id, project_id, name, status(SETTING_UP/TUNING/READY),
  embedding_model, graphrag_enabled, created_at

RagInstanceDocument
  id, rag_instance_id (FK), source_id (FK -> SourceData), 확장 대비 배열 형태로 취급

RagPipelineCandidate  (§3.1의 청킹×검색 조합)
  id, rag_instance_document_id (FK), chunking_strategy, retrieval_config,
  is_temporary(bool), selection_count, finalized(bool)
```

— §4.3의 `EmbeddingBenchmarkRun`/`Result`와는 별개 테이블(그건 "어떤 임베딩이 일반적으로 좋은가"를 축적하는 내부 R&D용, 이건 "이 RAG 인스턴스가 실제로 어떤 조합으로 완성됐는가"를 기록하는 제품 데이터).

### 5.6 처리 시간이 걸리는 단계의 UX — 진행 상태 표시 (비동기/폴링)

실제 청킹·임베딩이 수행되는 단계(§5.2 3단계: 자동 파싱 → 3개 청킹 후보 계산 → 각 후보 실제 청킹 → 각 조합 임베딩/인덱싱)는 문서 크기·조합 개수에 따라 초 단위가 아니라 **분 단위**로 걸릴 수 있다(§3.4에서 확인한 HF Inference API의 콜드스타트/재시도 지연까지 겹치면 더 길어질 수 있음). 이 구간에서 사용자가 "멈춘 건지 계속 되는 건지" 알 수 없는 상태로 방치되지 않도록 아래를 설계 원칙으로 둔다.

- **동기(블로킹) 처리 금지**: 업로드 후 파싱/청킹/임베딩은 프론트가 응답을 기다리며 화면을 막아두는 방식이 아니라, 백엔드에서 **비동기 작업(job)** 으로 처리하고 프론트는 작업 상태를 조회해 반영하는 구조로 간다 — 구체적으로는 **작업 상태를 폴링(polling)** 하는 방식으로 시작한다(실시간 스트리밍/웹소켓은 향후 개선 여지로 남기고, 최초 구현은 폴링으로 충분).
- **다른 작업 병행 가능**: 처리 중에도 사용자가 RAG 대시보드로 이동하거나 다른 RAG 인스턴스를 보는 등 **다른 작업을 계속할 수 있어야 한다** — 즉 처리 상태는 "이 화면을 벗어나면 안 되는 모달 잠금"이 아니라, 대시보드 목록의 해당 RAG 항목에 상태 배지(예: "청킹 중…", "임베딩 중… 2/3")로 표시되고, 화면을 나갔다 다시 들어와도 진행 상황이 이어서 보이는 형태여야 한다. (§5.3의 "설정 중" 상태 개념과 같은 선상 — 처리 중 상태도 대시보드에서 벗어나도 유지·조회 가능해야 함.)
- **단계별 진행 표시(프로그레스 바/스텝 인디케이터)**: 전체를 하나의 뭉뚱그린 스피너로 두지 않고, "파싱 중 → 청킹 후보 계산 중 → 후보 1/2/3 임베딩 중" 처럼 **현재 어느 단계인지**를 보여준다. 조합별 임베딩처럼 여러 개를 병렬/순차로 처리하는 구간은 "N개 중 M개 완료" 형태의 진행률을 노출한다.
- **완료된 조합부터 순차 공개**: 3개 청킹 후보를 모두 기다렸다가 한꺼번에 결과를 보여주는 게 아니라, **먼저 끝난 후보/조합부터 점진적으로(incremental) 화면에 나타나도록** 한다 — 전부 끝날 때까지 아무것도 안 보이는 것보다 체감 대기시간이 짧다.
- **재시도/지연 사유는 사용자에게 맥락 있게 노출**: §3.4에서 확인한 HF Inference API 콜드스타트로 인한 지연·재시도가 발생하면, 단순 로딩 스피너가 아니라 "모델을 준비하는 중입니다(잠시 더 걸릴 수 있어요)" 같은 **원인이 드러나는 메시지**를 보여준다 — 원인 불명의 정지 상태로 보이지 않게 하는 것이 핵심.
- **전송 방식은 용도별로 다르게 간다(폴링 vs 스트리밍, 웹소켓 아님)**:
  - **처리 상태(파싱/청킹/임베딩 진행률)**: 위에서 정한 대로 **폴링**. 업데이트 빈도가 수 초~수십 초 단위로 낮아 소켓의 즉시성 이득이 크지 않고, 기존 REST 인프라를 그대로 재사용해 구현·운영 복잡도가 낮다. 필요해지면(동시 사용자 규모 확대 등) 추후 전환 여지만 남겨둔다.
  - **실사용 채팅(§5.2a의 실사용 검색, §5.2의 튜닝 비교 채팅 모두)**: **스트리밍(SSE/HTTP streaming)** — 질문 1번에 답변이 토큰 단위로 흘러나오는 단방향 흐름이라 양방향 연결(웹소켓)까지는 필요 없다. "생성 중단" 같은 기능도 별도의 취소 요청으로 처리 가능해 소켓 인프라를 추가할 이유는 없다.

### 5.7 화면 구조 설계 (§5.4 레퍼런스 기반 최종안)

지금까지의 모든 결정(§3~§5.6)을 §5.4의 리서치 결과에 대입해 정리한, 실제 구현 시 참고할 화면 구조 스케치. 와이어프레임 수준이며, 실제 컴포넌트 매핑(어떤 공용 UI 컴포넌트를 쓸지)은 착수 시점에 다시 확정.

**시각적 스타일 방향**: 레이아웃 구조뿐 아니라 전반적인 비주얼 톤도 **NotebookLM 스타일**(여백을 넉넉히 쓰는 미니멀한 카드 표면, 절제된 포인트 컬러, 과하지 않은 그림자/보더)을 참고한다. 다만 완전히 새 디자인 시스템을 들이는 게 아니라, 플랫폼의 기존 공용 컴포넌트(`HanaCard`/`HanaBadge`/`HanaSelect` 등)와 디자인 토큰(색상/타이포)을 그대로 쓰되, 여백·카드 밀도·정보 위계를 NotebookLM에 가깝게 가져가는 정도로 적용한다 — 구체적인 스타일 가이드(여백 값, 색상 매핑 등)는 착수 시점에 확정.

**(A) RAG 대시보드** — Hugging Face Hub의 카드 그리드 패턴 + GitHub Actions의 "행별 상태 아이콘" 패턴을 결합.

```
┌─────────────────────────────────────────────────────────┐
│ RAG 대시보드                                [+ 새로 만들기] │
├─────────────────────────────────────────────────────────┤
│ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │
│ │ 출장비 규정 RAG │ │ 인사규정 RAG    │ │ 신규 RAG(●설정중)│   │
│ │ ● 준비완료      │ │ ◐ 임베딩 중 2/3 │ │ ○ 문서 대기      │   │
│ │ 문서 3 · BGE-M3 │ │ 문서 1 · Qwen3  │ │ 문서 0          │   │
│ └───────────────┘ └───────────────┘ └───────────────┘   │
└─────────────────────────────────────────────────────────┘
```
- 카드 하나 = RAG 인스턴스 하나. 상태 아이콘(●준비완료/◐처리중/○설정중/✕실패)은 폴링으로 갱신되는 요약 배지 — 클릭하지 않아도 목록에서 바로 스캔 가능(§5.6 "다른 작업 병행 가능" 요건을 대시보드 레벨에서 만족).
- "◐ 임베딩 중 2/3" 처럼 처리 중 카드는 분수형 진행률을 배지에 직접 노출(GitHub Actions에 없던 부분을 우리가 보강).

**(B) RAG 생성 화면 — 업로드 → 처리 → 비교 (§5.2 2~5단계, 한 화면 안에서 상태 전환)**

§5.2에서 이미 정한 대로 "화면 이탈 없이" 진행되므로, 아래 3개 상태는 **같은 화면 영역**이 순서대로 바뀌는 것이지 별도 라우트가 아니다.

```
[상태 1: 업로드 — 1단계(임베딩 질의)로 결정된 모델 아래, 문서 업로드 대기]
┌─────────────────────────────────────────────────────────┐
│ 임베딩 모델: BGE-M3 (질의로 결정됨)                          │
├─────────────────────────────────────────────────────────┤
│              📄  문서를 드래그하거나 클릭해서 업로드           │
└─────────────────────────────────────────────────────────┘

[상태 2: 처리 중 — §5.6의 단계별 진행 표시, 폴링으로 갱신]
┌─────────────────────────────────────────────────────────┐
│ 출장비_지급_규정.pdf                                        │
│  ✓ 파싱 완료                                                │
│  ✓ 청킹 후보 3개 계산 완료                                   │
│  ◐ 조합별 임베딩 중 (2/3 완료)                               │
└─────────────────────────────────────────────────────────┘

[상태 3: 비교(Compare) 모드 — 준비된 조합부터 자동 진입, 아래 참고]
```
- 상태 전환은 자동이다: 업로드 즉시 상태 2로 바뀌고, 처리(파싱→청킹 후보 계산→각 후보 청킹까지)가 끝나면 사용자 조작 없이 **자동으로 비교(Compare) 모드 화면으로 전환**된다 — 별도의 "다음" 버튼을 누르게 하지 않음.
- §5.6의 "완료된 조합부터 순차 공개" 원칙에 따라, 3개 후보를 전부 기다리지 않고 **먼저 준비된 후보만으로 비교 화면에 우선 진입**할 수도 있다(나머지는 비교 화면 안에서 뒤늦게 합류하는 컬럼으로 표시).

```
[동률 상태 — "완료" 버튼 비활성화]
┌─────────────────────────────────────────────────────────┐
│ 라운드 2                질문: "해외 출장 하루 식비 한도는?"  │
├───────────────────────┬───────────────────────┬─────────┤
│ ☐ 조항+일반 검색? 선택1회│ ☑ 조항+정밀 검색? 선택1회│ ☐ 문단+… 선택0회│
│ (스트리밍 중…)          │ 해외 출장 시 국가 등급  │           │
│                        │ (A/B/C)에 따라 1일     │           │
│                        │ 80~150달러입니다[근거1]│           │
│                        │ ⏱ 1.8초                │           │
│ [원본 조각만 보기]       │ [원본 조각만 보기]      │           │
├───────────────────────┴───────────────────────┴─────────┤
│  [ 다음 라운드 진행 ]        [ 완료 (동률이라 비활성화) ]    │
└─────────────────────────────────────────────────────────┘

[단독 1위 상태 — "완료" 버튼 활성화, 그래도 더 진행 가능]
┌─────────────────────────────────────────────────────────┐
│ 라운드 3                질문: "국내 출장 숙박비 한도는?"     │
├───────────────────────┬───────────────────────┬─────────┤
│ ☑ 조항+일반 검색? 선택1회│ ☑ 조항+정밀 검색? 선택3회│ ☐ 문단+… 선택0회│
│  …(답변 생략)…          │  …(답변 생략)…          │           │
├───────────────────────┴───────────────────────┴─────────┤
│  [ 다음 라운드 진행 ]              [ 완료(조항+정밀 검색) ]  │
└─────────────────────────────────────────────────────────┘

["완료" 클릭 후 — 최종 확인 화면]
┌─────────────────────────────────────────────────────────┐
│           이 조합으로 확정할까요?                          │
│                                                           │
│           조항 단위 청킹 + 정밀 검색                        │
│                                                           │
│  마지막으로 확인한 질문/답변                                 │
│  Q. 국내 출장 숙박비 한도는?                                │
│  A. 국내 출장 숙박비는 1박 10만원을 한도로 합니다[근거1]      │
│                                                           │
│           [ 다시 비교하기 ]     [ 확정하기 ]                │
└─────────────────────────────────────────────────────────┘
```
- **선택은 각 카드 자체의 체크박스**로 한다 — 카드마다 하단에 별도 "선택" 버튼을 두는 대신, 카드 우상단(또는 헤더)의 체크박스를 직접 체크/해제하는 방식. 여러 개 체크 가능(§5.3의 "1개 이상 선택" 요건을 체크박스로 자연스럽게 구현).
- **현재 라운드 번호**를 화면 상단에 항상 표시(예: "라운드 2")하고, **각 카드 헤더에 그 조합의 누적 선택 횟수**("선택 N회")를 함께 보여준다 — 사용자가 지금 몇 번째 비교 중인지, 어떤 조합이 앞서고 있는지 매 라운드 감을 잡을 수 있게 한다.
- **"다음 라운드 진행"과 "완료" 버튼을 둘 다 상시 노출**하고, 진행/완료 여부는 전적으로 사용자가 원할 때 누르는 것으로 한다(자동 전환 없음). 다만 §5.3의 규칙(동률이면 확정 불가)은 **"완료" 버튼의 활성/비활성**으로 강제한다 — 누적 선택 횟수가 동률인 동안은 "완료" 버튼이 비활성화(위 첫 번째 그림)되어 눌러도 진행되지 않고, 단독 1위가 생기면 그 즉시 버튼이 활성화되지만(위 두 번째 그림) 사용자는 원한다면 "완료"를 누르지 않고 "다음 라운드 진행"을 몇 번이든 더 눌러 계속 비교해볼 수 있다.
- **"완료" 클릭은 즉시 확정이 아니라 한 번 더 확인하는 중간 화면을 거친다**: 1위 조합명과, 사용자가 마지막으로 확인한 라운드의 질문+답변을 함께 보여줘 "내가 무엇을 확정하는지"를 다시 인지시킨다. "다시 비교하기"를 누르면 비교 화면으로 돌아가고, "확정하기"를 눌러야 비로소 §5.2a 상세 화면으로 전환되며 확정 처리(임시 조합 정리 등, §5.3)가 실행된다 — 되돌리기 어려운 액션(임시 데이터 삭제 포함)이라 한 번의 확인 단계를 둔다.
- "완료" 버튼 라벨에는 현재 1위 조합명을 함께 표시(예: "완료(조항+정밀 검색)")해, 눌렀을 때 무엇이 확정되는지 미리 알 수 있게 한다.
- **조합 개수(최대 6~9개, §3.3의 "청킹 후보 3개 × 고정2+가변1" 조합에서 나옴)에 맞춰 반응형으로 배치**: 고정된 3컬럼이 아니라, 실제 조합 개수에 따라 카드가 **자동으로 줄바꿈되는 그리드**로 배치한다(예: 화면 폭에 맞춰 한 줄에 3~4장씩, 남는 조합은 다음 줄로) — 모바일 대응은 없으므로(데스크톱 기준) 가로 스크롤보다는 줄바꿈 그리드가 더 자연스럽다. 개수가 적을 때(2~3개)는 한 줄, 많을 때(6~9개)는 2~3줄로 자연스럽게 늘어나는 형태.
- Chatbot Arena처럼 블라인드로 감추지 않고, §5.2b에서 정한 순화된 라벨을 **처음부터** 표시(우리 결정이 Arena와 다른 지점).
- 각 카드 하단에 §5.2c에서 정한 소요 시간·인라인 인용 칩·"원본 조각만 보기" 토글이 이미 반영된 형태.
- 하단 액션 바는 Arena의 "무승부/둘 다 별로" 컨셉 대신, 이미 §5.3에서 정한 **다중 선택(1개 이상)** 버튼 형태로 통일 — 굳이 별도 "둘 다 별로" 버튼을 추가하지 않고, 아무것도 선택하지 않고 다음 라운드로 넘어가는 것을 "둘 다 별로"와 동등하게 취급.

**(C) RAG 상세 화면 (§5.2a)** — NotebookLM의 "소스 사이드바 + 중앙 패널" 골격을 채택, 설정 스펙은 탭으로 분리.

```
┌───────────────┬───────────────────────────────────────┐
│ 문서 (검색범위)  │ [검색]  [설정 스펙]      ← 탭 전환      │
│ ☑ 출장비규정.pdf │─────────────────────────────────────│
│   조항+일반검색  │  Q: 국내 숙박비 한도는?                │
│ ☑ 복리후생.pdf   │  A: 1박 10만원입니다[근거1] ⏱ 1.2초    │
│   문단+정밀검색  │                                       │
│ [+ 문서 추가]    │  검색 민감도: 유연 [평이] 엄격          │
└───────────────┴───────────────────────────────────────┘
```
- 좌측 사이드바 = NotebookLM의 소스 목록과 동일한 역할: 문서 체크박스가 **검색 대상 범위 선택**(§5.2a)을 겸하고, 각 문서 항목에 확정된 조합이 작게 표시되며, "+문서 추가" 버튼이 §5.2a의 (a)/(b) 선택 플로우 진입점.
- 상단 탭 [검색] / [설정 스펙]으로 전환 — 스펙 조회는 §3 "혼동 방지" 원칙에 따라 검색(실사용) 화면과 완전히 분리된 읽기 전용 화면으로 별도 탭에 둔다(같은 화면에 섞지 않음).
- 선택된 문서들의 조합이 다를 때만 "검색 민감도"/조합 셀렉트가 노출(§5.2b, §5.2e) — 위 예시는 두 문서 조합이 달라 민감도 셀렉트가 보이는 경우.
- 인용은 Perplexity 패턴(번호 칩 → 호버 미리보기 → 클릭 시 사이드바의 해당 문서로 이동, NotebookLM의 "클릭 시 소스로 스크롤"과 동일한 상호작용)으로 통일.

---

## 6. 아직 결정되지 않은 것 (다음 대화에서 확정 필요)

- [ ] `HF_TOKEN` 발급/제공 방식 확정 (사용자가 직접 `.env`에 넣을지, 다른 방식인지)
- [ ] 1차 실험(임베딩 비교)에 쓸 대표 질의 세트를 누가/어떻게 작성할지 (수동 작성 vs 기존 golden set 재활용)
- [ ] §2.4(문서 타입별 3개 파이프라인 추천) 착수 시점 — §4(임베딩 벤치마크)보다 먼저 할지 나중에 할지
- [ ] 표준 벤치마크 데이터셋(MIRACL 등)을 실제로 코퍼스에 추가할 시점과 규모
- [ ] 임베딩 모델 선택용 questionnaire의 질문 문항/추천 로직 확정 (한국어/온프레미스/예산 외 추가 질문 필요한지)
- [ ] GraphRAG on/off questionnaire 문항 문구 및 노출 위치 (프로젝트 설정 페이지 vs RAG 포탈 내)
- [ ] 청킹×검색방식 비교 결과 저장 스키마(§3.3의 `retrieval_config` 메타데이터 포함) — §4.3과 유사한 형태로 `ChunkingBenchmarkRun`/`ChunkingBenchmarkResult` 신설 필요, 아직 초안 없음
- [ ] "검색 설정 변경 시 청킹 추천 재검증 필요" 경고를 실제로 어디서/어떻게 트리거할지(예: GraphRAG 토글 변경 이벤트 감지)
- [ ] 파싱 자동 트리거(§3.5) 구현 시점 — 업로드 즉시 vs RAG 포탈 첫 열람 시점 중 택1, 기존 수동 "Parse source" 버튼(재파싱용) 유지 여부
- [ ] **문서 전체 vs 샘플 비교 임계값 실측(§5.3)**: 정책은 확정(청크 개수 기준, 잠정 500개)했으나 이 숫자는 가설값 — "청킹 후보 하나당 실제 처리 시간"을 실측해 임계값을 확정해야 함
- [x] ~~"선택 횟수 최다 조합으로 확정"의 동점 처리 규칙~~ → §5.3/§5.7(B)에서 확정: 완료는 사용자가 원할 때 누르는 버튼이되, 동률인 동안 비활성화 처리(강제 반복 아님)
- [x] ~~미완성 RAG 인스턴스 정리(cleanup) 정책~~ → §5.3에서 확정: 원본 문서만 남기고 나머지는 폐기, 처음부터 재시작
- [ ] §5.5에서 스케치한 `RagInstance`/`RagInstanceDocument`/`RagPipelineCandidate` 데이터 모델 — 실제 마이그레이션 설계 전 리뷰 필요
- [x] ~~"임시 저장된" 미확정 조합의 청킹 결과를 확정 후 언제/어떻게 정리할지~~ → §5.3에서 확정: 확정 시점에 나머지 조합의 임시 청킹 결과는 삭제 처리
- [x] ~~RAG 상세 화면(§5.2a)에서 "문서 추가 → (b)이 문서는 따로 튜닝" 선택 시 튜닝 UI를 재사용할지 별도 모달로 넣을지~~ → §5.2a에서 확정: 같은 컴포넌트를 재사용(진입점만 다르게, 1단계는 건너뛰고 3단계부터 진입)
- [x] ~~§5.2b 검색 시 조합 셀렉트 UI의 구체적 표기 방식~~ → §5.2b에서 확정: 순화된 이름 + "?" 아이콘 호버 설명, clutter 우려는 드롭다운 특성상 기각(별도 처리 불필요)
- [x] ~~처리(파싱/청킹/임베딩) 대기 중 UI/UX 원칙~~ → §5.6에서 확정: 비동기 job + 폴링, 다른 작업 병행 가능, 단계별 진행률, 완료 조합 순차 공개, 재시도 사유 노출
- [ ] §5.6 폴링 주기(예: 2~3초 vs 5초)와 백엔드 job 상태 모델(예: `RagInstance.status`에 세부 단계를 얼마나 세분화해 저장할지) 구체 설계 — 전송 방식(폴링) 자체는 확정, 주기/스키마는 미정
- [x] ~~처리 상태 조회 및 실사용 채팅의 전송 방식(폴링/스트리밍/웹소켓)~~ → §5.6에서 확정: 처리 상태=폴링, 채팅(튜닝 비교 포함)=스트리밍(SSE), 웹소켓은 채택하지 않음
- [x] ~~비교 화면(4단계)에서 원본 청크를 보여줄지 LLM 답변을 보여줄지~~ → §5.2c에서 확정: 기본은 LLM 답변 + 근거 청크 노출, "원본 조각만 보기" 뷰 전환 토글 제공
- [x] ~~답변 내 출처 표기 방식~~ → §5.2c에서 확정: 답변 아래 나열이 아니라 문장 안 인라인 각주/배지로 표시
- [x] ~~근거 부족 시 hallucination 방지 정책~~ → §5.2e에서 확정: 유사도 등 관련도 기준 미달 시 "관련 문서를 찾지 못했습니다"로 답하도록 강제
- [x] ~~실사용 중 피드백 수집 여부~~ → §5.2e에서 확정: 👍/👎 선택 기능으로 제공(필수 아님), 부정 피드백 누적 시 재튜닝 권장 알림에 활용
- [ ] §5.2e "근거 부족" 판단 기준(유사도 임계값 등 구체 수치)과 재튜닝 권장 알림의 부정 피드백 임계값 — 아직 미정
- [x] ~~UI/UX 레퍼런스 딥리서치 및 화면 구조 설계~~ → §5.4(리서치 결과)·§5.7(대시보드/생성-비교/상세 화면 와이어프레임)에서 확정
- [ ] §5.7 와이어프레임을 실제 공용 UI 컴포넌트(HanaCard/CompactTable/HanaSelect/HanaBadge 등)에 어떻게 매핑할지 — 착수 시점에 확정 필요
- [x] ~~조합 개수가 많을 때(6~9개)의 배치 방식~~ → §5.7(B)에서 확정: 고정 컬럼이 아니라 화면 폭에 맞춰 줄바꿈되는 반응형 그리드(데스크톱 기준)
- [x] ~~전반적인 비주얼 스타일 방향~~ → §5.7에서 확정: NotebookLM 스타일(여백/미니멀 카드) 참고, 단 기존 공용 컴포넌트·디자인 토큰은 유지
- [ ] §5.7 시각적 스타일의 구체 수치(여백/카드 밀도 등) — 방향만 확정, 상세 스타일 가이드는 착수 시점에 확정
- [x] ~~"완료" 클릭 시 바로 확정할지 확인 단계를 둘지~~ → §5.7(B)에서 확정: 1위 조합명 + 마지막 라운드 질문/답변을 보여주는 최종 확인 화면을 거친 뒤 "확정하기"를 눌러야 실제 확정
- [ ] Hugging Face Inference Playground의 다중 모델 비교 레이아웃은 리서치로 세부사항 확인 못함(§5.4 미검증 표시) — 필요시 재조사
